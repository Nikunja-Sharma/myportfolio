# EmailJS Configuration
# Copy this file to .env.local and replace with your actual EmailJS credentials

# Service ID from EmailJS dashboard (e.g., service_abc123)
VITE_EMAILJS_SERVICE_ID=your_service_id_here

# Template ID from EmailJS dashboard (e.g., template_xyz789)
VITE_EMAILJS_TEMPLATE_ID=your_template_id_here

# Public Key from EmailJS dashboard (e.g., user_abcdef123456)
VITE_EMAILJS_PUBLIC_KEY=your_public_key_here

# Note: These are environment variables for Vite (prefixed with VITE_)
# They will be available in the browser, so only use public keys here
# Never put private keys or sensitive data in these variables